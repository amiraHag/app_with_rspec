<% provide(:title, 'All Cars') %>
<% provide(:head_quote, 'All Avaliable car') %> 
    <td colspan="2" rowspan="2" align="left" valign="top">
    	<p class="style24">
    		You are here 
    		<%= link_to "All cars",    '#' %>.
    		</p>
    		<% if signed_in? %>
      <h1>Listing cars</h1>

<table bgcolor="EECCEE" border="4px">
  <tr>
    <th>Carmodel</th>
    <th>Price</th>
    <th>Myear</th>
    <th>Uyear</th>
    <th>Cartype</th>
    <th>User</th>
    <th colspan="3"></th>
   
  </tr>

<% @cars.each do |car| %>
  <tr>
    <td><%= car.carmodel %></td>
    <td><%= car.price %></td>
    <td><%= car.myear %></td>
    <td><%= car.uyear %></td>
    <td><%= car.cartype %></td>
    <td><%= car.user_id %></td>
    <td><%= link_to 'Show', car %></td>
    <td><% if  false %><%= link_to 'Edit', edit_car_path(car) %> <% end %> </td>
    <td><% if false %><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %><% end %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car', new_car_path %>
<% else %>
please  <%= link_to "Sign In",    signin_path %> or <%= link_to "Register",    register_path %>
 <% end %>
</td>
  </tr>
 
